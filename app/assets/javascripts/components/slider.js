
define(['jquery'], function ($) {

    return {
        init: function (domId, config) {
            /*var defaultOptions = {
                sliderId: domId,
                effect: "slide",
                autoAdvance: true,
                pauseOnHover: true,
                pauseTime: 5000,
                speed: 500,
                startSlide: 0,
                circular: true,
                touchCircular: true,
                mobileNav: false,
                before: null,
                after: null
            };

            new NinjaSlider($.extend(defaultOptions, config));*/

        }
    }

});

